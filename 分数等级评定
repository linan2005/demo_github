#分数等级评定

print("请输入0-100之间的分值")
score = float(input("请输入分数:"))

if 0 <= score < 60:
    level = "D"
elif 60 <= score < 80:
    level = "C"
elif 80 <= score < 90:
    level = "B"
elif 90 <= score < 100:
    level = "A"
elif score == 100:
    level = "S"
else:
    level = "你输入的分数有误，请重新运行输入!"
print(level)
